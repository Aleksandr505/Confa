name: confa

services:
  caddy:
    image: caddy:2
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks: [ shared_web ]

  livekit:
    image: livekit/livekit-server:latest
    container_name: livekit
    restart: unless-stopped
    command: [ "--config","/etc/livekit/livekit.yaml" ]
    volumes:
      - ./livekit.yaml:/etc/livekit/livekit.yaml:ro
    networks: [ shared_web ]
    ports:
      - "3478:3478/udp"
      - "50000-50050:50000-50050/udp"

  api:
    image: ${API_IMAGE}
    container_name: api
    env_file:
      - .env
    environment:
      - APP_PORT=${APP_PORT}
      - APP_NAME=${APP_NAME}
      - ACTIVE_PROFILES=${ACTIVE_PROFILES}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - PASSWORD_ENCODER_SECRET=${PASSWORD_ENCODER_SECRET}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ACCESS_EXPIRATION=${JWT_ACCESS_EXPIRATION}
      - JWT_REFRESH_EXPIRATION=${JWT_REFRESH_EXPIRATION}
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET}
      - INIT_BOOTSTRAP_SERVICE_KEY=${INIT_BOOTSTRAP_SERVICE_KEY}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - CLIENT_BASE_URL=${CLIENT_BASE_URL}
      - AVATAR_MAX_UPLOAD_BYTES=${AVATAR_MAX_UPLOAD_BYTES:-2097152}
      - AVATAR_PRESIGN_TTL_SECONDS=${AVATAR_PRESIGN_TTL_SECONDS:-900}
      - AVATAR_S3_ENDPOINT=${AVATAR_S3_ENDPOINT:-http://minio:9000}
      - AVATAR_S3_REGION=${AVATAR_S3_REGION:-eu}
      - AVATAR_S3_BUCKET=${AVATAR_S3_BUCKET:-confa-assets}
      - AVATAR_S3_ACCESS_KEY=${AVATAR_S3_ACCESS_KEY:-minioadmin}
      - AVATAR_S3_SECRET_KEY=${AVATAR_S3_SECRET_KEY:-minioadmin}
      - AVATAR_S3_PATH_STYLE=${AVATAR_S3_PATH_STYLE:-true}
    expose:
      - "8080"
    networks: [shared_web]
    depends_on:
      mysql:
        condition: service_healthy
      liquibase-migrations:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      minio:
        condition: service_started

  client:
    image: ${CLIENT_IMAGE}
    container_name: client
      #ports:
      #- "3000:80"
    expose:
      - "80"
    networks: [shared_web]

  admin-client:
    image: ${ADMIN_CLIENT_IMAGE}
    container_name: admin-client
    #ports:
    #- "3000:80"
    expose:
      - "80"
    networks: [ shared_web ]

  mysql:
    image: mysql:8.0
    container_name: mysql
    env_file:
      - .env
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_ROOT_PASSWORD}
      - MYSQL_DATABASE=${DB_NAME}
      - MYSQL_USER=${DB_USERNAME}
      - MYSQL_PASSWORD=${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    networks: [shared_web]
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: redis
    command: ["redis-server", "--save", "20", "1", "--loglevel", "warning"]
    networks: [shared_web]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: minio
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=${AVATAR_S3_ACCESS_KEY:-minioadmin}
      - MINIO_ROOT_PASSWORD=${AVATAR_S3_SECRET_KEY:-minioadmin}
    volumes:
      - minio_data:/data
    expose:
      - "9000"
      - "9001"
    networks: [shared_web]
    restart: unless-stopped

  liquibase-migrations:
    image: liquibase/liquibase:latest
    container_name: liquibase-migrations
    depends_on:
      mysql:
        condition: service_healthy
    networks: [ shared_web ]
    volumes:
      - ../backend/api/src/main/resources:/liquibase/changelog:ro
      - ./libs/mysql-connector-j-8.3.0.jar:/liquibase/lib/mysql-connector-java.jar
    env_file:
      - .env
    command:
      - --driver=com.mysql.cj.jdbc.Driver
      - --url=jdbc:mysql://mysql:3306/${DB_NAME}
      - --changeLogFile=space/confa/api/db/changelog/liquibase-changelog.yaml
      - --username=${DB_USERNAME}
      - --password=${DB_PASSWORD}
      - update
    restart: "no"

  ots:
    image: onetimesecret/onetimesecret:latest
    env_file:
      - .env
    environment:
      - RACK_ENV=production
      - HOST=pass.confa.space
      - SSL=true
      - SECRET=${OTS_SECRET}
      - REDIS_URL=redis://:${REDIS_PASSWORD}@ots-redis:6379/0
      - AUTH_SIGNUP=false
    depends_on:
      - ots-redis
    networks: [shared_web]
    restart: unless-stopped

  ots-redis:
    image: redis:bookworm
    command: [ "redis-server", "--requirepass", "${REDIS_PASSWORD}" ]
    volumes:
      - ots_redis_data:/data
    networks: [shared_web]
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
  mysql_data:
  redis_data:
  minio_data:
  ots_redis_data:

# Before start docker-compose create network:
# docker network ls
# docker network create shared_web
networks:
  shared_web:
    driver: bridge
