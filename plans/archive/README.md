# Archived Plans

Completed plans live here only when they are still useful for historical context.

Do not use archived plans as the source of truth for current behavior. Update durable documentation in `docs/` or module-local files instead.

